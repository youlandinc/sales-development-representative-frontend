'use client';
export const fetchCache = 'force-no-store';

import { SetPassword } from '@/components/molecules';

const SetPasswordPage = () => {
  return <SetPassword />;
};

export default SetPasswordPage;
