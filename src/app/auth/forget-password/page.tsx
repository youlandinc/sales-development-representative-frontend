'use client';
export const fetchCache = 'force-no-store';

import { Container } from '@mui/material';

const ForgetPassword = () => {
  return <Container>I am forget password</Container>;
};

export default ForgetPassword;
