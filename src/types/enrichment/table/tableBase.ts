// ============================================================================
// Table Base Types (shared across table domain)
// ============================================================================

export interface RunRecordItem {
  recordIds: string[];
  isAll: boolean;
}
