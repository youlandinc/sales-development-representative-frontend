export enum EmailDomainStateEnum {
  PENDING = 'PENDING',
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED',
  ACTIVE = 'ACTIVE',
}

export enum CampaignsPendingTimeLineEnum {
  scheduled = 'SCHEDULED',
  completed = 'COMPLETED',
}
