import { Stack } from '@mui/material';

export const CampaignProcessContentLunch = () => {
  return (
    <Stack
      border={'1px solid'}
      flex={1}
      sx={{
        transition: 'all .3s',
      }}
    ></Stack>
  );
};
