import { Stack } from '@mui/material';

export const CampaignProcessContentMessaging = () => {
  return (
    <Stack
      border={'1px solid'}
      flex={1}
      sx={{
        transition: 'all .3s',
      }}
    ></Stack>
  );
};
